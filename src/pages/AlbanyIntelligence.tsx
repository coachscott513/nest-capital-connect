import TownPageTemplate from "@/components/TownPageTemplate";
import { albanyData } from "@/data/townData";

const AlbanyIntelligence = () => {
  return <TownPageTemplate town={albanyData} />;
};

export default AlbanyIntelligence;
