import TownPageTemplate from "@/components/TownPageTemplate";
import { amsterdamData } from "@/data/townData";

const AmsterdamIntelligence = () => {
  return <TownPageTemplate town={amsterdamData} />;
};

export default AmsterdamIntelligence;
