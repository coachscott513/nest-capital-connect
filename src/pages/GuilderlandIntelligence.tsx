import TownPageTemplate from "@/components/TownPageTemplate";
import { guilderlandData } from "@/data/townData";

const GuilderlandIntelligence = () => {
  return <TownPageTemplate town={guilderlandData} />;
};

export default GuilderlandIntelligence;
